interface:
  display_name: "Engineering Task"
  short_description: "Run Polaris only when explicitly invoked"
  default_prompt: "Use $engineering-task to qualify and execute this engineering request through Polaris."

policy:
  allow_implicit_invocation: false
