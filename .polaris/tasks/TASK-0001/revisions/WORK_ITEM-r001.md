# Work Item

Readable projection only. The matching `revisions/work-item-rNNN.json` is authoritative.

## TASK-0001 — 编写 Polaris 完整使用说明书

目标是新增一份连续、可操作的中文手册，回答使用者从接入项目到提出需求、配合 Review、跨电脑恢复和升级排错的全过程问题。

范围仅包含 `docs/USAGE.md` 和 README 导航，不修改 Polaris 协议或运行行为。任务严谨度为 R0，七项验收标准以同目录的 `work-item-r001.json` 为准。
